<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="heart.mapper.LoginMapper">
    <select id="getUsername" parameterType="int" resultType="heart.model.LoginModel">
        SELECT heart_user.id, heart_user.username, heart_user.password
        FROM heart_user
        WHERE heart_user.id = #{id,jdbcType=VARCHAR}
    </select>
   
</mapper>
